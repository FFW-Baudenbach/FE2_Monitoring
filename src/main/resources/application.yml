spring:
  main:
    banner-mode: off

management:
  endpoints:
    web:
      exposure:
        include:
          - info
          - health
  endpoint:
    health:
      show-details: NEVER
  info:
    java:
      enabled: true
  security:
    enabled: false